<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learning.travelingassistant.mapper.CommentMapper">

    <resultMap id="commentResultMap" type="com.learning.travelingassistant.entity.Comment">
        <id property="id" column="id"/>
        <result property="attractionId" column="attraction_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="imageUrl" column="image_url"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="findByAttractionId" resultMap="commentResultMap">
        SELECT id, attraction_id, user_id, content, image_url, create_time
        FROM comment
        WHERE attraction_id = #{attractionId}
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.learning.travelingassistant.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (attraction_id, user_id, content, image_url, create_time)
        VALUES (#{attractionId}, #{userId}, #{content}, #{imageUrl}, NOW())
    </insert>

    <select id="selectByAttractionIdWithUser" resultType="map">
        SELECT
            c.id,
            c.attraction_id AS attractionId,
            c.user_id AS userId,
            c.content,
            c.image_url AS imageUrl,
            c.create_time AS createTime,
            u.username
        FROM comment c
        LEFT JOIN users u ON c.user_id = u.id
        WHERE c.attraction_id = #{attractionId}
        ORDER BY c.create_time DESC
    </select>

</mapper>
