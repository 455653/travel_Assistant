<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learning.travelingassistant.mapper.UserMapper">

    <select id="findByUsernameAndPassword" resultType="com.learning.travelingassistant.entity.User">
        SELECT id, username, password, real_name, id_card, phone, email, create_time, update_time
        FROM users
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="findByUsername" resultType="com.learning.travelingassistant.entity.User">
        SELECT id, username, password, real_name, id_card, phone, email, create_time, update_time
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByPhone" resultType="com.learning.travelingassistant.entity.User">
        SELECT id, username, password, real_name, id_card, phone, email, create_time, update_time
        FROM users
        WHERE phone = #{phone}
    </select>

    <select id="findByIdCard" resultType="com.learning.travelingassistant.entity.User">
        SELECT id, username, password, real_name, id_card, phone, email, create_time, update_time
        FROM users
        WHERE id_card = #{idCard}
    </select>

    <insert id="insert" parameterType="com.learning.travelingassistant.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, real_name, id_card, phone, email, create_time, update_time)
        VALUES (#{username}, #{password}, #{realName}, #{idCard}, #{phone}, #{email}, NOW(), NOW())
    </insert>

</mapper>
