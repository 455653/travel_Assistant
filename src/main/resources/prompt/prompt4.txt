最后，我们需要实现前后端的完整交互逻辑。请编写 JavaScript 代码来处理以下流程：

1. **点击省份：** 当用户在 ECharts 3D 地图中点击某个省份（如“湖北”）时：
   - 调用后端接口 `/api/provinces` 查找对应的 ID（或者前端维护一个映射）。
   - 调用 `/api/cities/{provinceId}` 获取城市列表。
   - 使用 jQuery 动态渲染右侧面板，生成城市按钮列表。

2. **选择城市：** 当用户点击右侧的“武汉市”按钮时：
   - 调用 `/api/attractions/top/{cityId}`。
   - 清空右侧面板，渲染景点卡片列表（显示图片、名称、热度）。

3. **查看详情：** 当用户点击景点图片时：
   - 调用 `/api/attractions/{id}/detail`。
   - 弹出一个 Modal 模态框，展示景点详细介绍和下方的评论列表。

请提供整合后的 JavaScript 代码逻辑。