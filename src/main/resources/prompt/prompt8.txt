# Role
你是一名资深的 Java 全栈开发工程师。我们正在开发“中国旅游助手”项目。
目前项目后端基于 Spring Boot + MyBatis，前端基于 HTML + jQuery + Three.js。

# Goal (本次任务目标)
我需要对“景点详情”功能进行重大升级：
1.  **数据库变更**：在 `attraction` 表中新增“旅游攻略”字段。
2.  **前端重构**：废弃原本的 Modal 弹窗展示，改为点击景点后跳转到一个**全新的独立详情页 (`detail.html`)**。
3.  **页面布局**：详情页需要包含头部信息和**四个 Tab 标签页**切换功能。

# 1. Database Changes (数据库变更)
目前的 `attraction` 表结构如下：
```sql
create table attraction
(
    id          bigint auto_increment comment '景点ID' primary key,
    city_id     bigint                                  not null comment '所属城市ID',
    name        varchar(100)                            not null comment '景点名称',
    image_url   varchar(500)                            null comment '景点图片URL',
    description text                                    null comment '景点描述',
    view_count  int           default 0                 null comment '浏览次数',
    rating      decimal(3, 1) default 0.0               null comment '评分(0.0-5.0)',
    create_time timestamp     default CURRENT_TIMESTAMP null comment '创建时间',
    constraint attraction_ibfk_1 foreign key (city_id) references city (id) on delete cascade
) comment '景点表' charset = utf8mb4;
请提供以下 SQL 语句：

ALTER TABLE 语句：新增一个名为 strategy_guide 的字段，类型为 TEXT，注释为 '旅游攻略'。

UPDATE 语句：为现有的测试数据（如黄鹤楼、武汉大学等）更新一些模拟的 strategy_guide 文本数据，以便我测试展示效果。

2. Backend Updates (后端代码更新)
请基于新增的数据库字段，更新后端代码：

Entity: 更新 Attraction.java 实体类，增加 strategyGuide 属性。

Mapper: 更新 AttractionMapper.xml 中的 ResultMap 和查询语句，确保查询详情时能返回 strategy_guide 字段。

注意：AttractionDetailDTO 可能直接复用了 Attraction 实体，请确保 DTO 也能携带攻略数据。

3. Frontend Development (前端开发)
请创建一个全新的页面 detail.html，并修改 index.html 的跳转逻辑。

A. 详情页 (detail.html) 布局要求
参考类似携程或马蜂窝的详情页风格，但保持简约：

顶部导航栏：复用 home.html 的导航栏样式。

头部区域 (Header)：

左侧：景点大图（image_url）。

右侧：景点名称（大标题）、评分（⭐）、浏览量（👁️）、一句话简介（截取部分 description）。

核心内容区 (Tab System)：

请实现一个 Tab 选项卡组件，包含以下 4 个标签：

【景点介绍】：展示完整的 description。

【旅游攻略】：展示新增的 strategy_guide 数据。

【天气】：目前后端没有天气数据，请在此 Tab 下显示一个静态占位符：“🌤️ 实时天气接口对接中...”。

【游客评论】：展示该景点的评论列表（复用之前的评论渲染逻辑）。

交互要求：点击 Tab 标签，下方内容区域无刷新切换。

B. 现有页面 (index.html 或 index-threejs.html) 修改
找到点击左侧“景点卡片”的 JavaScript 逻辑。

修改前：调用 openAttractionModal(id) 打开弹窗。

修改后：直接跳转新页面 window.open('detail.html?id=' + id, '_blank');。

Deliverables (交付物)
请提供以下代码：

数据库变更 SQL (ALTER + UPDATE)。

更新后的 Attraction.java 和 AttractionMapper.xml 片段。

完整且包含 CSS/JS 的 detail.html 代码。

index.html 中需要修改的那一小段 JavaScript 跳转逻辑。

请确保 detail.html 的 CSS 风格与 home.html 保持一致（蓝白色调，清爽现代）。